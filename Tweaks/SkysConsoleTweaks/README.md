## Features
### Error Screen
- Replaces line wrapping with horizontal scrolling.
### Console
- Replaces line wrapping with horizontal scrolling.
- Removes the stack trace from copied messages.
- Removes the need to clear old messages to reduce lag.
- Breaks the help menu into multiple blocks for searchability.

<hr>

Requires `HarmonyForLogicWorld` by `Ecconia`.
